const ADD = 'contacts/Add';

const DELETE = 'contacts/Delete';

const VERIFICATE = 'contacts/Verificate';

const SHOW = 'contacts/Show';

const CHANGE_FILTER = 'contacts/ChangeFilter';

const FORM_SUBMIT = 'contacts/FormSubmit';

const contactsTypes = {
  ADD,
  DELETE,
  VERIFICATE,
  SHOW,
  CHANGE_FILTER,
  FORM_SUBMIT,
};

export default contactsTypes;
